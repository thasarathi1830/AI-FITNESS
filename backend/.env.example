# MongoDB Atlas Configuration
# Get your connection string from: https://cloud.mongodb.com
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/fitness_app?retryWrites=true&w=majority

# JWT Secret Key
# Generate a secure random key using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or the app will auto-generate one on first run and display it in the console
JWT_SECRET=your-auto-generated-jwt-secret-will-appear-in-console

# Google Gemini API Key
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
